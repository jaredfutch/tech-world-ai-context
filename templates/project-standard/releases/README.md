# Release Evidence

Store one record per meaningful deployment/release. Use `RELEASE.template.md`.

Release evidence documents what actually happened. It should identify the exact commit/artifact, environment, migrations, checks, backup state, rollback path, limitations, and acceptance state.

Do not place credentials, tokens, secret configuration values, private customer data, or unnecessary sensitive logs in release records.
